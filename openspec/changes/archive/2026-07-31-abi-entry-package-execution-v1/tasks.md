## 0. Prerequisite dependency

- [ ] 0.1 BLOCKING, confirmed not satisfied: the prerequisite OpenSpec change `abi-exchange-instrument-identity-v1`
      (owning `src/exchange/exchangeSymbolResolver.ts`'s `ExchangeSymbolResolver`
      interface and its production implementation, design.md §9) has landed in this
      repository before starting any task in group 5, or the symbol-dependent parts of
      group 7, that requires resolving a real Bybit `symbol` from a Runtime `ticker` in
      production. Groups 1-4, 6, and the non-symbol-dependent parts of 7-9 do not depend
      on it and may proceed first.

## 1. Internal domain types and semantics mapper

- [x] 1.1 Add `src/domain/entryOrderSemantics.ts`: pure `mapEntryOrderSemantics(side: "long" | "short") -> { exchangeSide: "Buy" | "Sell", triggerDirection: "rises_to" | "falls_to" }` implementing the V1 table (`long → Buy, falls_to`; `short → Sell, rises_to`), with no market-price parameter in its signature and a comment noting it is scoped to the currently supported EMA-pullback entry geometry (design.md §2).
- [x] 1.2 Add `src/correlation/entryPackageExecutionRecord.ts`: `EntryPackageExecutionRecord` type (including `ticker`, `exchange_symbol`, `risk_multiplier`), `status` union (including `terminal_unfilled`), `EarlyExecutionObservation` type, `BindingHistoryEntry` type (including `exchange_symbol` and the `exchange_terminal` end reason), per design.md §5.
- [x] 1.3 Unit tests for the mapper: long→Buy/falls_to, short→Sell/rises_to, no market-price dependency in the function signature, deterministic across repeated calls with the same input.

## 2. Correlation repository and durability

- [x] 2.1 Add `src/correlation/entryPackageCorrelationRepository.ts` implementing the durable write sequence from design.md §4: serialize full record → single `write()` with trailing newline → explicit `fsync` (`fsPromises.open` → `handle.appendFile` → `handle.sync()` → `handle.close()`) → update in-memory indexes → resolve.
- [x] 2.2 Implement an internal FIFO write queue serializing all physical appends across keys (design.md §4), independent of the keyed mutex from group 7.
- [x] 2.3 Implement startup replay: read the correlation file, keep only the last valid line per composite key, tolerate a truncated final line, fail readiness on any non-final corrupt line (design.md §4/§13, audit §11/§14). Post-implementation review correction: a syntactically-valid-but-wrong-shaped line is now also treated as corruption via a runtime shape guard (`isValidEntryPackageExecutionRecord`), not just a JSON parse failure — see design.md Decision 1.
- [x] 2.4 Build in-memory indexes at replay/write time: composite key → record, `order_link_id` → record (including historical `binding_history` entries), `order_id` → record.
- [x] 2.5 Add `ABI_ENTRY_PACKAGE_CORRELATION_PATH` to `src/config/config.ts` (new file path, sibling to `journalPath`, not reusing it).
- [x] 2.6 Unit tests: durable write sequence ordering, crash-during-append (truncated tail) recovery, non-final corruption fails readiness, composite/`order_link_id`/`order_id` lookups including historical bindings, restart restores current and historical bindings. Post-implementation review correction, added: a wrong-shaped-but-JSON-valid line fails readiness the same way malformed JSON does, and is tolerated only as a final (truncated-write) line.

## 3. Order identity/generation

- [x] 3.1 Add `src/domain/entryPackageOrderIdentity.ts`: `buildEntryPackageOrderLinkId(strategyInstanceId, tradeCycleId, role, generation) -> string`, following the existing `sha256(...).slice(0, N)` + prefix pattern, verified to stay under Bybit's documented 36-character `orderLinkId` limit.
- [x] 3.2 Encode the generation rules from design.md §3 / audit §7 in the application service (group 7): generation is read from the correlation record, not re-derived; first create reserves generation 1; amend-in-place keeps the same generation; cancel-and-create reserves the next generation; retry of an ambiguous/timed-out attempt reuses the already-reserved generation.
- [x] 3.3 Unit tests: distinct trade cycles of the same strategy instance produce distinct identities; identical inputs are deterministic; identity changes only with generation, not with any other field.

## 4. Instrument rules and placeholder sizing

- [x] 4.1 Add `getInstrumentInfo(symbol: string): Promise<unknown>` to the `BybitAdapter` interface, `RestBybitAdapter` (unsigned `GET /v5/market/instruments-info`), and `StubBybitAdapter`. Post-implementation review correction: every `bybitAdapter.ts` REST call (not only this one) now carries `AbortSignal.timeout(config.bybitRequestTimeoutMs)`, sourced from a new `ABI_BYBIT_REQUEST_TIMEOUT_MS` config value (default 10000ms) — a hung call could otherwise hold a confirmation attempt and the keyed mutex indefinitely. See design.md §10.
- [x] 4.2 Add `src/exchange/instrumentTradingRulesProvider.ts`: `InstrumentTradingRulesProvider` port and a Bybit-backed implementation parsing `lotSizeFilter.minOrderQty`/`.qtyStep`/`.minNotionalValue`, with lazy per-resolved-symbol lookup and an in-memory TTL cache (design.md §7).
- [x] 4.3 Failure to fetch rules for a symbol surfaces as `internal_error` for that command only, never as a whole-service readiness failure (design.md §7).
- [x] 4.4 Add `src/risk/positionSizeCalculator.ts`: `PositionSizeCalculator` port (`calculate(ticker, plannedEntryPrice, initialStopPrice, riskMultiplier, context) -> calculatedQuantity`) and `FixedMinimumPositionSizeCalculator` implementing `max(qty_by_min, qty_by_notional)` with round-up-to-`qty_step` exact-decimal arithmetic (design.md §8), no `Number()` conversion at any step.
- [x] 4.5 Add `test/fakes/fakeInstrumentTradingRulesProvider.ts`, modeled on `test/fakes/fakeBybitAdapter.ts`.
- [x] 4.6 Extend `test/fakes/fakeBybitAdapter.ts` with a fake `getInstrumentInfo` and a fake `getOrderHistory` (design.md §10); extend `test/fixtures/config.ts` with defaults for the new config fields.
- [x] 4.7 Unit tests: min-qty-only case, min-notional-driven case (qty_by_notional > qty_by_min and vice versa), exact-decimal division correctness (no float conversion), rules-unavailable fails only the current command, `risk_multiplier` is accepted and threaded through without affecting the V1 result, no hardcoded quantity literal appears in the application service (static/contract check).

## 5. Bybit payload/adapter extensions

- [x] 5.1 Add `BybitGetOrderHistoryPayload` type and `getOrderHistory(payload)` to `BybitAdapter`/`RestBybitAdapter`/`StubBybitAdapter`, querying `GET /v5/order/history` by `orderLinkId` (design.md §10) — needed because `getOrderByLinkId`'s `/v5/order/realtime` cannot see orders that have already left the open/pending set.
- [x] 5.2 Add a new entry-package payload builder in `src/exchange/bybitOrderMapper.ts` (or an adjacent new file) that reuses `mapSide`/`mapTriggerDirection` and constructs Bybit create/amend/cancel payloads from entry-package inputs (desired entry, calculated quantity, resolved symbol, order identity) — without calling or modifying `mapExecutionPlanToBybit` or the legacy `ExecutionPlan` type.
- [ ] 5.3 BLOCKED on prerequisite `abi-exchange-instrument-identity-v1` (confirmed not landed, task 0.1): cannot import a production `ExchangeSymbolResolver` that does not exist in this repository. `mapEntryPackageToBybit`/`InstrumentTradingRulesProvider` already consume an already-resolved `symbol: string` parameter (never a raw `ticker`), satisfying this task's structural requirement; only the real resolver injection at the composition root remains undone, per design.md Decision 9's explicit sequencing.
- [x] 5.4 Unit tests: payload builder produces correct `triggerDirection`/`side`/`tpslMode`/quantity fields for representative long and short cases (both always include `initial_take_price`, since it is a required field of the entry-package contract — there is no without-take-profit case to test here); confirms `mapExecutionPlanToBybit` is not called by any new code path (static/contract check).

## 6. Package confirmation

- [x] 6.1 Add `src/services/entryPackage/packageConfirmation.ts`: bounded-retry confirmation (starting from the existing `2`-attempt/`300ms` pattern in `verifyPostCreateProtection.ts`, tunable) that queries `getOrderByLinkId` (`/v5/order/realtime`) and diffs the returned `triggerPrice`/`qty`/`stopLoss`/`takeProfit` against the desired package.
- [x] 6.2 When the realtime query finds nothing or reports a terminal status, fall back within the same bounded attempt to `getOrderHistory` (task 5.1) to determine the fill/rejection/deactivation outcome (design.md §10).
- [x] 6.3 Classify into the audit §26 outcomes: pending order confirmed, full fill before acknowledgement, partial fill before acknowledgement, rejected/deactivated before any fill, ambiguous observation. Post-implementation review correction: split into six outcomes, not five — `not_found` (every query cleanly answered, nothing found anywhere) is now distinct from `ambiguous` (a query failure, or a found-but-unclassifiable result), because collapsing them let a REST query failure during cancel confirmation fabricate `entry_package_absent`. See design.md §10.
- [x] 6.4 On full/partial fill, extract and return an aggregate `EarlyExecutionObservation` (`order_status`, `cumulative_filled_qty`, `remaining_qty`, `avg_execution_price?`, `observed_at`) without reconstructing individual fills. Post-implementation review correction: a filled/partially-filled order's present-and-mismatched `qty`/`triggerPrice`/`stopLoss`/`takeProfit` is no longer blindly trusted (`fillFieldsPlausible`) — see design.md §10.
- [x] 6.5 On ambiguous observation, surface a result that the application service (group 7) maps to `internal_error`, never a success. Post-implementation review correction: `not_found` maps to `internal_error` the same way on a first send, but additionally lets group 7's repeat-PUT path safely resend the in-flight action — see design.md §11.
- [x] 6.6 Unit tests for each of the six outcomes (including `not_found`), including at least one that only resolves via the order-history fallback (e.g. a fully-filled-and-closed order absent from the realtime query), confirmation of cancel (expects not-found or terminal-cancelled status from either query), and a REST query failure during cancel confirmation never resolving to `cancelled_confirmed`.

## 7. EntryPackageApplicationService

- [x] 7.1 Add `src/concurrency/keyedMutex.ts`: `withKeyLock(key, fn)` chaining promises per key in a `Map`, in-process only (design.md §6); release the lock in a `finally` so a rejected/thrown `fn` still frees the key for the next request.
- [x] 7.2 Add `src/services/entryPackage/entryPackageApplicationService.ts` implementing the flow from design.md §11: acquire keyed mutex → load correlation record → classify (ticker-mismatch reject / metadata-only durable-update-and-revalidate / CREATE / REPLACE / CANCEL / CONFIRM-ABSENT / repeat-PUT) → (for CREATE/REPLACE) map semantics → calculate quantity → build payload → persist provisional record (including `ticker`, `exchange_symbol`, `risk_multiplier`) → call guarded `execution.ts` function → confirm → persist confirmed/failed state → release mutex → serialize result. NOTE: `resolveSymbol` is an injected `(ticker: string) => string` function, not a production `ExchangeSymbolResolver` (still blocked on the prerequisite, task 0.1/5.3).
- [x] 7.3 Reject a request whose `ticker` differs from the trade cycle's already-recorded `ticker` with a safe error, without any exchange call (design.md §5/§11).
- [x] 7.4 Implement the metadata-only path: when only `source_plan_bar_open_time_ms`/`locked_exit_profile` differ from the stored desired entry, durably update the record and run bounded revalidation of the existing order without sending an amend or create request.
- [x] 7.5 Implement REPLACE routing: amend-in-place when only price/qty/stop/take change; cancel-and-create (new generation) when side changes. Post-implementation review correction: cancel-and-create now bounded-confirms the old order is actually gone (`confirmEntryPackageCancelled`) before building/sending the new order — a bare cancel REST acceptance is not proof the old order stopped being live — and amend reuses the record's already-stored `exchange_symbol` instead of re-resolving the ticker. See design.md §11.
- [x] 7.6 Implement repeat-PUT bounded revalidation for `status = "applied"`: classify into pending-matches / fill-observed / terminal-without-fill / contradictory, per spec's "Repeat requests ... truthfully revalidated" requirement. Post-implementation review correction: for a record not yet `applied` (a create/amend whose outcome was never durably confirmed, e.g. after a crash), a `not_found` confirmation now resends the in-flight action (tracked via the new `pending_action` field) instead of dead-ending in `unknown` forever; `risk_multiplier` is also now re-persisted on every revalidation path, not only on create/amend. See design.md §5/§11.
- [x] 7.7 Implement the `terminal_unfilled` fail-closed path: non-null repeat PUT while `terminal_unfilled` returns `internal_error` without creating a new order (record the old generation's `binding_history` entry with `end_reason: "exchange_terminal"`); a `desired_entry: null` PUT while `terminal_unfilled` is permitted and transitions to `absent`; a subsequent non-null PUT after confirmed `absent` starts a normal CREATE with a new generation.
- [x] 7.8 Treat a `skipped_live_execution` result from `execution.ts`'s guarded functions as a non-success: return `internal_error`, never `entry_package_applied` or `entry_package_absent`.
- [x] 7.9 Ensure every other non-success path (transport failure, confirmation failure, ambiguous state, durability write failure) returns one of the existing public error responses and never a fabricated success.
- [x] 7.10 Unit/integration tests (against `FakeBybitAdapter` + fakes from group 4): first APPLY, identical repeated APPLY, REPLACE via amend, REPLACE via cancel-and-create, successful CANCEL, already-absent CANCEL, create transport failure, create accepted but confirmation failed, terminal-without-fill fail-closed then CANCEL then fresh CREATE, ticker-mismatch rejected without an exchange call, metadata-only change updates without amend/create, `skipped_live_execution` never produces a success, concurrent identical PUT produces one exchange order, concurrent differing PUT does not interleave, and a failing first request releases the mutex so a subsequent request for the same trade cycle still proceeds. Post-implementation review correction, added: cancel-and-create aborts creating the new order when the old one's cancellation is not confirmed (fill discovered or ambiguous); a create whose outcome was never confirmed self-heals via resend on a later identical repeat PUT, reusing the same identity; a `risk_multiplier`-only repeat PUT is durably persisted; amend reuses the record's stored `exchange_symbol` even if the injected resolver would now return something different.

## 8. Route/composition wiring

- [x] 8.1 Update `src/routes/entryPackageRoutes.ts` to check the entry-package readiness flag (design.md §13) and, when ready, call `EntryPackageApplicationService` instead of unconditionally returning `internalErrorResult()`, using the existing `serializeAppliedEntryPackage`/`serializeAbsentEntryPackage` for output; when not ready, return the existing safe `internal_error` response.
- [x] 8.2 Update `src/app/server.ts` to construct and wire the new correlation repository, instrument rules provider, position size calculator, keyed mutex, application service, and readiness flag, leaving `signalRoutes`/`intentRoutes`/`accountRoutes` wiring unchanged. NOTE: `resolveSymbol` is wired to a function that throws (blocked on the prerequisite, task 0.1/5.3) — production Bybit calls remain non-functional until that lands, exactly as proposal.md discloses. Post-implementation review correction: `startServer` now also logs a startup warning that `entryPackageReady` on `GET /health` only reflects correlation-store replay health, not resolver/production readiness — this gap is otherwise only visible by reading code.
- [x] 8.3 Make correlation-store replay run asynchronously at startup (`src/app/server.ts`) without delaying `server.listen()` for legacy/account routes; set the readiness flag to `true` only once replay succeeds, and keep it `false` (fail closed) on any non-final corruption (design.md §13).
- [x] 8.4 Add an `entryPackageReady` field to `GET /health` in `src/routes/systemRoutes.ts`, without changing any existing field.
- [x] 8.5 Verify (by reading the diff, not just by tests passing) that `entryPackageRoutes.ts` does not touch correlation state, Bybit, or the mutex directly.

## 9. Tests and new entry-package smoke scripts

- [x] 9.1 Run the full unit/integration suite added in groups 1-8 and confirm coverage of the acceptance-matrix categories in `docs/ENTRY_PACKAGE_EXECUTION_AUDIT.md` §18: identity/concurrency, durability, confirmation, repeated PUT, sizing, readiness boundary, pure mapping, stability, layering, V1 scope.
- [x] 9.2 Add a new entry-package smoke script (`scripts/smoke-entry-package-contract-matrix.sh` + `scripts/fake-abi-entry-package-smoke-server.mjs`) exercising apply → replace-via-amend → replace-via-cancel-and-create → cancel → already-absent against a fake smoke server (production Bybit remains blocked on the prerequisite, so demo/testnet is not reachable yet), mirroring `smoke-sandbox-contract-matrix.sh`'s/`smoke-contract-matrix-fake.sh`'s structure without modifying either file.
- [x] 9.3 Add the new script's `npm run` entry (`smoke:entry-package:fake`) to `package.json` alongside the existing `smoke:*` scripts, without changing any existing script.
- [ ] 9.4 PARTIALLY VERIFIED, not fully confirmed: `smoke:contract:fake` (which wraps `smoke-sandbox-contract-matrix.sh` against the fake server) was run and passes end-to-end, and no `LEGACY_ONLY` file was touched (diff-confirmed). But `smoke:sandbox:contract`, `smoke:sandbox:amend`, `smoke:sandbox:order`/`smoke:testnet:order`, `smoke:sandbox:read` require live Bybit testnet credentials and explicit `ABI_CONFIRM_TESTNET_WRITE=YES` not available in this environment and were never actually run — "still pass" is not established for those four, only "unmodified."
- [x] 9.5 Run `npm test`. 139/139 passing.
- [x] 9.6 Run `npm run typecheck`. Clean.
- [x] 9.7 Run `npm run build`. Clean.
- [x] 9.8 Review the full diff to confirm: no edits to any file `docs/LEGACY_SIGNAL_INTENT_DISPOSITION.md` marks `LEGACY_ONLY` (confirmed via `git status` against the disposition table); no new fields added to the public entry-package request/response DTOs (`src/domain/entryPackageApi.ts` has zero diff); no fabricated `2xx` on any error path (no hardcoded `statusCode` in the application service — only the existing serializers); no production implementation of `ExchangeSymbolResolver` was added to this change (`src/exchange/exchangeSymbolResolver.ts` does not exist; it must come from the prerequisite, task 0.1).
