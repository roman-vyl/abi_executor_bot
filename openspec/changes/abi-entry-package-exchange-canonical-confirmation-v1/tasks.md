## 1. Confirmation Semantics

- [x] 1.1 Confirm accepted create/amend commands from strict same-order identity, recognized state, matching quantity, and structurally valid exchange fields without raw desired-price equality.
- [x] 1.2 Apply the same identity/state/quantity model to repeat PUT and metadata-only revalidation.

## 2. Regression Coverage

- [x] 2.1 Add create and amend regressions using the real Demo canonical price read-back shape.
- [x] 2.2 Preserve focused fail-closed coverage for identity, quantity, state, malformed numeric fields, and query failures.

## 3. Verification

- [x] 3.1 Run strict OpenSpec validation and the full repository test, typecheck, build, and OpenAPI verification commands.
